# Copy this file to .env and fill in the secrets/URLs.

# Firecrawl API key (required for Research panel)
FIRECRAWL_API_KEY=

# Optional: Excalidraw collab server URL (for future live collaboration)
# Example: https://your-excalidraw-room.example.com
EXCALIDRAW_ROOM_URL=

# Supabase (planned next)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI Provider (OpenRouter preferred; OpenAI-compatible)
# For now the UI only exposes GPT via OpenRouter.
# Keep these values and it will work out of the box.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional but recommended per OpenRouter guidelines
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=APO

# Optional: direct OpenAI (not exposed in UI yet)
# If you really want to bypass OpenRouter, set both of these
# and change AI_MODEL to a native OpenAI model slug like "gpt-4o-mini".
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Default model (OpenRouter slug)
AI_MODEL=openai/gpt-4o-mini

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_MODEL=openai/gpt-4o-mini
